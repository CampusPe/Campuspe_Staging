# 🚨 CRITICAL: Azure Environment Variables - CORRECTED

# ==========================================
# REQUIRED FIXES FOR AZURE DEPLOYMENT
# ==========================================

# 1. Database (MUST be set with real credentials)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/campuspe-staging?retryWrites=true&w=majority

# 2. Server Configuration 
PORT=8080
NODE_ENV=production

# 3. JWT Secret (MUST be set)
JWT_SECRET=your-super-secure-jwt-secret-for-production

# 4. WABB Configuration (CORRECTED URLS)
WABB_API_KEY=your-actual-wabb-api-key
WABB_WEBHOOK_URL=https://api.wabb.in/api/v1/webhooks-automation/catch/220/HJGMsTitkl8a/
WABB_WEBHOOK_URL_RESUME=https://api.wabb.in/api/v1/webhooks-automation/catch/220/ORlQYXvg8qk9/
API_BASE_URL=https://campuspe-api-staging.azurewebsites.net

# 5. AI Services (MUST be set with real keys)
CLAUDE_API_KEY=sk-ant-api03-your-real-claude-api-key
ANTHROPIC_API_KEY=sk-ant-api03-your-real-anthropic-api-key

# 6. CORS Configuration
CORS_ORIGIN=https://dev.campuspe.com,https://campuspe-web-staging-erd8dvb3ewcjc5g2.southindia-01.azurewebsites.net,http://localhost:3000

# ==========================================
# DEPLOYMENT STEPS TO FIX
# ==========================================

# Step 1: Set Environment Variables in Azure Portal
# - Go to: Azure Portal → campuspe-api-staging → Configuration → Application settings
# - Update ALL variables above with REAL values (not placeholders)

# Step 2: Fix These Specific Variables (I can see they're wrong in your screenshot):
# REMOVE these incorrect variables:
# - WABB_WEBHOOK_URL_GENERAL (not needed)
# - WABB_WEBHOOK_URL_JOBS (not needed) 
# - WABB_WEBHOOK_URL_OTP (not needed)

# KEEP only these 4 WABB variables:
# - WABB_WEBHOOK_URL=https://api.wabb.in/api/v1/webhooks-automation/catch/220/HJGMsTitkl8a/
# - WABB_WEBHOOK_URL_RESUME=https://api.wabb.in/api/v1/webhooks-automation/catch/220/ORlQYXvg8qk9/
# - WABB_API_KEY=your-real-key
# - API_BASE_URL=https://campuspe-api-staging.azurewebsites.net

# Step 3: Redeploy
# - Save configuration in Azure Portal
# - Restart the App Service
# - Wait for GitHub Actions to complete deployment

# Step 4: Test
# curl https://campuspe-api-staging.azurewebsites.net/api/health
