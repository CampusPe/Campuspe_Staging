{
  "name": "campuspe-staging",
  "version": "1.0.0",
  "description": "CampusPe Platform - Staging Environment",
  "main": "apps/api/dist/app.js",
  "scripts": {
    "start": "cd apps/api && npm start",
    "build": "cd apps/api && npm run build && cd ../web && npm run build",
    "install-deps": "npm install && cd apps/api && npm install && cd ../web && npm install"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "dependencies": {
    "cross-env": "^7.0.3"
  }
}
